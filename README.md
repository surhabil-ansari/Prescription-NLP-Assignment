# Prescription-NLP-Assignment
This project builds a hybrid NLP pipeline to extract structured information from raw prescription text. It combines text preprocessing and rule-based (regex) extraction to identify fields like medicine name, dosage, frequency, and duration. The system converts unstructured medical text into clean JSON output.
